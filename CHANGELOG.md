## v2.12.0
- 修复切换调用起点后子提供商缓存错位，连接会话和 API Key 轮询状态现在按面板原始序号稳定复用
- 缩小提供商配置指纹范围，修改预设、白名单等无关配置时不再重建全部 HTTP 会话
- 缺少接口地址或 API Key 的提供商会直接跳过并进入下一项，避免无意义网络请求
- 支持同名提供商按序号切换；名称存在歧义时提示改用序号
- `active_provider` 纳入命令优先动态配置，提升重启后的切换结果稳定性

## v2.11.0
- 新增 `#提供商列表`，管理员可查看提供商启停状态、接口模式、模型和当前调用起点
- 新增 `#切换提供商 <序号或名称>`，切换后从指定项开始调用，失败时循环回退到其余启用项
- 新增 `#切换提供商 自动`，恢复管理面板中的默认优先级顺序
- 切换结果持久化保存，插件重启后仍然生效

## v2.10.0
- 移除每日签到命令、签到配置和签到数据加载逻辑
- 新增多个模型提供商配置，支持为每项独立设置接口模式、地址、Key、默认模型和文生图模型
- 提供商调用失败、异常或返回空结果时按配置顺序自动后退，成功后立即停止
- 插件停用或卸载时统一关闭所有提供商的 HTTP 会话

## v2.9.0
- 修复 `#切换模型` 在管理员 ID 类型不一致或旧模型列表格式异常时无响应的问题
- 统一 URL 自动补全：基础地址中的 v1/v1beta 和完整接口尾部会被忽略，再按所选模式生成唯一请求地址
- 新增 OpenAI Images、OpenAI Chat、Gemini 官方、自定义完整路径四种接口模式
- 配置面板移除旧 Generic/Gemini 独立地址、Key 池和 api_mode，仅保留统一接口配置；旧配置仍可静默读取
- `#切换API模式 generic` 等旧指令写法继续兼容，并映射到新的接口模式
- 自动识别提示词中的常规宽高比与 1K/2K/4K，并填充 OpenAI `size` 或 Gemini `imageConfig`
- 文生图默认比例可配置为 4:3，图生图会自动识别原图比例
- 补充 aiohttp、Pillow、PyMuPDF 插件依赖声明

## v2.8.8
- 优化上下文逻辑修改配置文件写入逻辑
